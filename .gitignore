# Generated: ~10MB concatenation of every per-region entities.json.
# Regenerate with `uv run scripts/build.py`. The canonical data
# (data/<region>/entities.json) is committed, so this is just a convenience bundle.
data/all-entities.json

# Python / uv
__pycache__/
*.pyc
.venv/

# playwright mcp scratch
.playwright-mcp/
